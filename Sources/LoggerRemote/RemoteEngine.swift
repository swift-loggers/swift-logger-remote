/// Placeholder type for the future remote-delivery engine.
///
/// M3.4 currently locks the contract value types only. No public
/// engine dispatch API is committed yet.
public actor RemoteEngine {
    public init() {}
}
